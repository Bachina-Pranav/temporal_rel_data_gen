# V4.1 Hierarchical Diffusion Results

This report is populated after running the VM experiments. The implementation writes machine-readable results to:

```text
outputs/amazon-toy/conditional_tabdlm_hierarchical_v41/comparison.csv
outputs/amazon-toy/conditional_tabdlm_hierarchical_v41/comparison.json
```

## Experiment Matrix

| Model | Graph | Structured conditioning | Valid generative baseline |
| ----- | ----- | ----------------------- | ------------------------- |
| flat v4 baseline | correct | implicit simultaneous denoising | yes |
| hierarchical v4.1 | no graph | generated | yes |
| hierarchical v4.1 | correct graph | generated | yes |
| hierarchical v4.1 | shuffled graph | generated | yes, diagnostic graph ablation |
| hierarchical v4.1 | zero graph | generated | yes, diagnostic graph ablation |
| hierarchical v4.1 | correct graph | oracle structured | no, diagnostic only |
| LSTM v5.3 | existing best setting | generated by LSTM | yes |

## Main Comparison Table

Run `src/scripts/run_hierarchical_v41_ablation.py` to fill this table from `comparison.csv`.

| Model | Graph | Structured conditioning | Shape ↓ | Table C2ST ↓ | Text C2ST ↓ | Trend ↓ | Review KS ↓ | Runtime |
| ----- | ----- | ----------------------- | ------: | -----------: | ----------: | ------: | ----------: | ------: |
| pending | pending | pending | pending | pending | pending | pending | pending | pending |

## Attribute-To-Text Analysis

Answer after evaluation:

- Does explicit structured conditioning improve summary generation?
- Does it improve review-text semantic fidelity?
- Does it improve rating-summary dependence?
- Does it improve rating-review dependence?
- Does generated conditioning approach oracle structured conditioning?
- How much error propagates from stage 1?

## Graph Analysis

Answer after evaluation:

- Does correct graph context outperform zero graph?
- Does correct graph context outperform shuffled graph?
- Which metrics benefit from graph context?
- Is graph conditioning worth its runtime cost?

## Recommendation

Recommendation is intentionally pending until the generated-table ablations have been run. Do not claim graph conditioning is useful solely from logit-sensitivity diagnostics.

